## v1.1.0 검증

- `npm test`: 4개 테스트 통과. 웜업 null RIR 허용, 본세트·보조운동 필수 RIR 유지, 값 범위 검증, 기존 schema·체크섬 호환, JSON 직렬화 왕복, 완료 세트 기반 부위 집계, 부위 수동 지정, 12/24회 경계, 수정·삭제 재계산, 수동 1RM 구분 및 날짜 역순 처리를 확인했습니다.
- `node --check app.js`: 통과.
- 브라우저 UI, 실제 IndexedDB 트랜잭션, 오프라인 동작은 이번 환경에서 미검증입니다. Playwright용 Chromium 설치를 시도했으나 다운로드 시간 초과로 실행하지 못했습니다.
- 기존 DB 이름과 버전, 원장 트랜잭션·snapshot·복구 보호 로직은 유지했습니다. 리포트는 운동 저장 트랜잭션에 원장 데이터와 함께 포함됩니다.

# 테스트 보고서 · v1.0.1

검증일: 2026-09-09.

- Node.js: 자동 테스트 **38개 통과, 실패 0개**.
- 실제 Chrome IndexedDB 통합 테스트: **20개 통과, 실패 0개**.
- 브라우저 화면 조작: 초기 설정, 자유운동 복수 부위·세트 입력·저장, 새로고침 후 유지, 척도 입력 임시 저장·복원·완료, 캘린더 날짜 조회, 과거 척도 기록 수정, 데이터 진단 화면을 확인했습니다.
- 모바일 CSS 뷰포트: 메인 화면 320·360·390·430px 가로 넘침 없음. 320px 캘린더·척도 수정·데이터 화면 조작 및 가로 넘침 없음.

검증은 격리된 테스트 브라우저에서 합성 운동 기록으로 수행했습니다. 저장소 통합 테스트는 `record-qa-<임의 식별자>`라는 별도 IndexedDB를 사용했고 실사용 원본을 건드리지 않았습니다. 브라우저 저장소를 모의 객체로 대체하지 않았습니다.

## 이번에 발견·수정한 문제

1. **첫 실행 UUID 호환성:** 테스트 브라우저의 HTTP 문맥에서 `crypto.randomUUID`가 없어 첫 시작이 실패했습니다. `getRandomValues` 기반 UUID v4 호환 처리를 추가했습니다.
2. **체크섬 호환성:** `crypto.subtle`이 없는 문맥에서도 같은 SHA-256을 계산하도록 호환 구현을 추가했습니다. 빈 문자열, 한글, 0–129바이트 패딩 경계, 100KB 입력을 Node Web Crypto 결과와 대조했습니다. HTTPS에서 지원되면 native Web Crypto를 우선 사용합니다.
3. **저장 직전 손상:** 앱을 연 뒤 원본이 손상되어도 새 저장으로 덮지 않도록 저장 전 검증과 transaction 내 원본 비교를 추가했습니다.
4. **복구 확인 중 다른 탭 변경:** 미리보기 이후 생성된 새 기록을 이전 백업으로 덮어쓰지 않도록 확인 전 원본과 쓰기 시점 원본을 비교합니다.
5. **입력 보호:** 수동 1RM 변경 시 진행 중인 세션을 화면 상태에서 없애지 않으며, 완료 저장 중 입력 변경과 중복 작업을 차단합니다.

DB 이름과 DATA_SCHEMA_VERSION 1은 유지했습니다. APP_VERSION은 1.0.1, CACHE_VERSION은 app-shell-2입니다.

## 실제 IndexedDB 테스트 20개

| 번호 | 항목 | 결과 |
|---|---|---|
| 1 | 최초 open은 빈 원본을 자동 저장하지 않음 | 통과 |
| 2 | 명시적 최초 설정·DB 재조회 검증 | 통과 |
| 3 | 6사이클 완료 기록 저장·다음 순번 계산 | 통과 |
| 4 | 자유운동 단일 부위 저장 | 통과 |
| 5 | 복수 부위 수정·3대 진행 상태 불변 | 통과 |
| 6 | DB 연결 종료 후 새 연결에서 기록·부위 복원 | 통과 |
| 7 | JSON 내보내기·검증·미리보기 데이터 일치 | 통과 |
| 8 | 과거 삭제 전 snapshot 및 재계산 | 통과 |
| 9 | JSON 백업 복구 후 이벤트 전체 일치 | 통과 |
| 10 | 잘못된 JSON 거부 후 원본·정상 모드 유지 | 통과 |
| 11 | 임시 기록 저장·읽기, 완료 횟수 불변 | 통과 |
| 12 | 수동 1RM 변경은 임시 기록을 삭제하지 않음 | 통과 |
| 13 | 다른 연결의 오래된 revision 쓰기 거부 | 통과 |
| 14 | DB read 실패 주입 시 저장 중지·원본 불변 | 통과 |
| 15 | checksum 손상 감지·snapshot 복구 | 통과 |
| 16 | 원본 누락·사용 흔적 존재 시 빈 초기화 차단 | 통과 |
| 17 | 미지원 schema/손상 원본 거부·보존 | 통과 |
| 18 | 복구 미리보기 이후 변경되면 교체 거부 | 통과 |
| 19 | 저장 직전 손상 검출·덮어쓰기 금지 | 통과 |
| 20 | 긴급 미러·유효 snapshot checksum 검증 | 통과 |

연결 종료는 IndexedDB 연결 객체를 닫고 새 객체로 다시 여는 검사입니다. 브라우저 프로세스 강제 종료나 기기 재부팅 시험과 같지는 않습니다. DB read 오류·손상은 테스트 DB에 의도적으로 주입했습니다.

## 화면 조작에서 확인한 실제 값

- 자유운동: 가슴·등·팔, QA 케이블컬 15kg × 12회, RIR 2 저장. 재실행 후 누적 1회·선택부위 유지, 다음 추천 스쿼트 척도 유지.
- 사이클 건너뛰기: 데드 척도를 직접 선택, 95kg × 6회/RIR 1 저장. 다음 추천은 스쿼트 근비대.
- 임시 저장: 척도 입력 후 새로고침 → 이어서 기록 → 입력 복원 → 완료 저장 성공.
- 과거 수정: 캘린더에서 95kg × 6회/RIR 1을 7회로 수정. e1RM 117.2 → 120.3kg, 실제 1RM은 120kg 유지.
- 진단: 이벤트 2개, 6사이클 1회, 자유운동 1회, revision 4, 긴급 미러 존재, 버전 1.0.1/schema 1 확인.

## 요청한 34개 항목의 검증 수준

| 요청 번호 | 항목 | 상태 |
|---|---|---|
| 1 | 최초 실행 | 실제 브라우저 통과 |
| 2 | 6사이클 저장 | 실제 DB·화면 통과 |
| 3 | 종료·재실행 유지 | 페이지 재실행·DB 연결 재생성 통과. 프로세스 강제 종료 미검증 |
| 4 | 여러 운동 저장 | 실제 DB·화면 통과 |
| 5 | 캘린더 조회 | 실제 화면 통과 |
| 6 | 자유로운 사이클 선택 | 실제 화면·로직 통과 |
| 7 | 재실행 후 progression | 페이지 재실행·DB 재계산 통과 |
| 8–11 | 자유운동 선택·단일/복수 부위·저장 | 실제 DB·화면 통과 |
| 12–14 | 자유운동 복원·선택부위·순서 불변 | 실제 DB·화면 통과 |
| 15–21 | 척도·PR·새 1RM·근비대 증량 | Node 로직 통과. PR 모든 화면 분기 실조작은 미검증 |
| 22 | 과거 수정 | 실제 DB·화면 통과 |
| 23 | 과거 삭제 | 실제 DB 통과. 삭제 확인 버튼 실조작은 미검증 |
| 24 | 전체 재계산 | 로직·실제 DB·척도 수정 화면 통과 |
| 25 | JSON export | 실제 DB의 export 데이터 검증 통과. 화면에서 다운로드 요청 실행 |
| 26 | JSON import | 실제 DB 검증·미리보기·transaction 복구 통과. OS 파일 선택부터의 전체 UI 흐름은 미검증 |
| 27 | 잘못된 JSON 거부 | 실제 DB 원본 보존까지 통과 |
| 28 | read 실패 시 자동 초기화 금지 | 실제 DB를 대상으로 오류 주입 검사 통과 |
| 29 | schema migration | v1 무손실 검증·미지원 버전 거부 통과. 새 schema 변환기는 아직 없음 |
| 30 | cache update 후 DB 유지 | API 분리 코드 검사·worker 모의 테스트. 실제 HTTPS 미검증 |
| 31 | 오프라인 실행 | worker fallback 모의 테스트. 실제 HTTPS·기기 미검증 |
| 32 | worker 업데이트 후 기록 유지 | 승인 활성화 모의 테스트. 실제 HTTPS 미검증 |
| 33 | cache 삭제 후 DB 유지 | 별도 저장소 구조 검토. 실제 브라우저 캐시 삭제 시험 미실시 |
| 34 | 320–430px UI | CSS 뷰포트 크기·DOM·입력 동작 검사. Android 키보드·실기기 미검증 |

## 아직 남은 한계

테스트 접속이 HTTP 문맥이어서 서비스워커, 영구 저장 승인, storage estimate 등 HTTPS 문맥 기능을 실제로 확인하지 못했습니다. 전체 페이지 캡처는 시간 초과됐으나 뷰포트 캡처는 성공했습니다. 320px 데이터 화면에서 하단 메뉴와 버튼의 한글 줄바꿈 문제를 발견해 메뉴 한 줄 유지·단어 단위 줄바꿈으로 보완했습니다. 모든 화면의 픽셀 단위 검수를 완료했다는 의미는 아닙니다.

실제 Android에서 새 HTTPS 배포 주소를 열고 설치 → 테스트 기록 저장 → 앱 종료·재실행 → JSON 파일 확인 → 비행기 모드 재실행 → 캐시 버전 업데이트 후 기록 유지를 확인해야 합니다. 이 검증에 실사용 기록을 삭제하는 방식은 사용하지 마세요.
